pre-commit:
  commands:
    lint-format:
      glob: "*.{js,ts,jsx,tsx,mjs,cjs,json,jsonc,css,md,yml,yaml}"
      run: pnpm exec biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true
