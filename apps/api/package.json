{
	"name": "api",
	"description": "API app",
	"dependencies": {
		"@effect/platform": "^0.96.1",
		"effect": "^3.21.1",
		"wrangler": "^4.61.1"
	},
	"devDependencies": {
		"@token-quest/biome-config": "workspace:^",
		"@token-quest/typescript-config": "workspace:^",
		"@types/node": "^25.6.0",
		"typescript": "5.9.3"
	},
	"private": true,
	"scripts": {
		"cf-typegen": "wrangler types",
		"lint": "biome check --write .",
		"check": "tsc && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev"
	}
}
